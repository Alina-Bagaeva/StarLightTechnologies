```markdown
# Отчетность по остаткам денежных средств и структуре статей ДДС

## 📊 **1. Ежедневный отчет по остаткам денежных средств (Ostatki.sql)**

### 🎯 **Основная цель запроса**
Создание **ежедневного отчета по остаткам денежных средств** на расчетных счетах и депозитах организаций за период с 31 декабря 2024 года по текущую дату.

### 📋 **Общая логика работы**
Запрос объединяет данные из двух источников:
1. **Остатки на расчетных счетах и депозитах** (таблица `OstatkiDeneg`)
2. **Движение денежных средств** (таблица `DvizhenieDS`)

### 🔧 **Пошаговое описание работы кода**

#### **1. Генерация календаря дат**
```sql
calendar AS (...)
```
- Создает последовательность всех дат от `2024-12-31` до **сегодняшнего дня включительно**
- Использует функцию `numbers()` и `dateDiff()` для генерации непрерывного временного ряда

#### **2. Подготовка уникальных комбинаций счетов**
```sql
dim_accounts AS (...)
```
- Извлекает уникальные комбинации: **БД + Организация + Расчетный счет** из обеих таблиц (`OstatkiDeneg` и `DvizhenieDS`)
- Для депозитов (`EtoDepozit = true`) формируется специальное название счета: `'55 счёт ' + Организация`
- Использует `UNION DISTINCT` для исключения дубликатов между таблицами

#### **3. Создание полной сетки данных**
```sql
full_grid AS (...)
```
- Создает **Декартово произведение**: каждая уникальная комбинация × каждая дата из календаря
- Гарантирует наличие записи **для каждого дня** для каждой комбинации счета

#### **4. Подготовка данных остатков**
```sql
ostatki as (...)
```
- Преобразует данные из таблицы `OstatkiDeneg` в удобный формат
- Для депозитов автоматически формируется название счета: `'55 счёт ' + Организация`
- Конвертирует `ПериодМСК` в дату

#### **5. Подготовка данных движения денежных средств**
```sql
postuplenia_viplaty AS (...)
```
- Агрегирует операции движения денег по **дате, БД, организации и счету**
- Использует `SUMIf()` для разделения сумм на:
  - **Поступления** (`EtoPostuplenie = true`)
  - **Выплаты** (`EtoPostuplenie = false`)
- Для депозитов формируется специальное название счета аналогично предыдущим шагам

#### **6. Финальный отчет**
```sql
SELECT ... FROM full_grid f LEFT JOIN ostatki o ... LEFT JOIN postuplenia_viplaty pv ...
```
**Формирует финальную таблицу с колонками:**

| Колонка | Описание |
|---------|----------|
| `date` | Дата отчета |
| `bd` | База данных |
| `Organizatsiya` | Организация |
| `RaschetniiSchet` | Расчетный счет или '55 счёт + Организация' для депозитов |
| `SummaOstatok_BEGIN` | **Остаток на начало дня** |
| `Postuplenie` | Сумма поступлений за день |
| `Viplata` | Сумма выплат за день |
| `SummaOstatok_END` | **Остаток на конец дня** |

**Особенности расчета:**
- Для `2024-12-31`: `SummaOstatok_BEGIN = 0`, `SummaOstatok_END = Postuplenie`
- Для остальных дней: 
  - `SummaOstatok_BEGIN = Вчерашний остаток - Поступления + Выплаты`
  - `SummaOstatok_END = Фактический остаток из таблицы OstatkiDeneg`

**Ключевые изменения в обновленной версии:**
1. **Более полное покрытие счетов**: `dim_accounts` теперь включает счета из обеих таблиц
2. **Исправление логики вычислений**: правильный расчет `SummaOstatok_BEGIN` через обратный пересчет
3. **Улучшенная обработка NULL**: использование `COALESCE` для всех числовых полей

### 📈 **Бизнес-ценность отчета**
- **Ежедневный мониторинг** остатков по всем счетам и депозитам
- **Анализ денежных потоков** (поступления vs выплаты)
- **Контроль ликвидности** организаций
- **Непрерывный временной ряд** без пропусков дат
- **Автоматическая консолидация** данных из разных источников

---

## 📈 **2. Отчет по остаткам с предыдущим днем (Ostatki_otchet.sql)**

### 🎯 **Основная цель запроса**
Создание **упрощенного отчета по остаткам** с автоматическим вычислением остатка на предыдущий день для каждой БД.

### 📋 **Общая логика работы**
Более простой вариант отчета, который фокусируется на:
1. **Агрегированных остатках по БД** (без детализации по организациям и счетам)
2. **Автоматическом расчете** остатка предыдущего дня

### 🔧 **Ключевые отличия от Ostatki.sql**
- Агрегирует остатки на уровне **БД**, а не отдельных счетов
- Использует **оконную функцию** `lagInFrame()` для получения остатка предыдущего дня
- Не включает движение денежных средств (поступления/выплаты)
- Более простой и быстрый для анализа на уровне БД

### 📊 **Структура результата**

| Колонка | Описание |
|---------|----------|
| `date_col` | Дата отчета |
| `BD` | База данных |
| `ostatok` | Остаток на текущую дату |
| `ostatok_prev_day` | Остаток на предыдущий день |

### 🎯 **Бизнес-применение**
- **Быстрый обзор** общей ликвидности по базам данных
- **Трендовый анализ** изменения остатков
- **Сравнение** между разными БД
- **Валидация** данных основного отчета

---

## 🏗️ **3. Иерархическое представление статей ДДС (Table_hierarcy.sql)**

### 🎯 **Основная цель запроса**
Создание **иерархического представления структуры статей ДДС** для аналитики и построения отчетов с группировкой по уровням.

### 📋 **Общая логика работы**
Преобразует плоскую таблицу статей ДДС в **древовидную структуру** с тремя уровнями группировки:
1. **Корневая папка** (root_folder) - самый верхний уровень
2. **Папка** (folder) - промежуточный уровень
3. **Папка1** (folder1) - дополнительный уровень группировки
4. **Статья ДДС** (StatyaDDS) - конечный элемент иерархии

### 🔧 **Пошаговое описание работы кода**

#### **1. Выделение групп статей**
```sql
gruppa AS (...)
```
- Выбирает все записи, которые являются группами (`EtoGruppa = true`)
- Сохраняет: название группы, ID группы и ID родительской группы

#### **2. Выделение конечных статей**
```sql
stati AS (...)
```
- Выбирает все записи, которые являются конечными статьями (`EtoGruppa = false`)
- Сохраняет: название статьи, ID родительской группы и ID статьи

#### **3. Построение иерархии**
```sql
SELECT ... FROM stati st LEFT JOIN gruppa g ... LEFT JOIN gruppa g1 ... LEFT JOIN gruppa g2 ...
```
- Строит иерархию до 3 уровней в глубину
- Использует **COALESCE** для заполнения пропусков в иерархии
- Формирует полный путь от корневой группы до конечной статьи

### 🌲 **Пример иерархии**

**Результат представления:**

| root_folder | folder | folder1 | StatyaDDS | StatyaID |
|-------------|--------|---------|-----------|----------|
| Операционная деятельность | Поступления | От покупателей | Продажа продукции А | ... |
| Операционная деятельность | Поступления | От покупателей | Продажа продукции Б | ... |
| Операционная деятельность | Выплаты | Поставщикам | Сырье и материалы | ... |
| Инвестиционная деятельность | Покупка ОС | Техника | Компьютеры | ... |

### 📈 **Бизнес-ценность представления**
- **Группировка статей ДДС** по логическим разделам
- **Многоуровневая детализация** от общего к частному
- **Единая структура** для всех аналитических отчетов
- **Упрощение фильтрации** и агрегации данных по группам

---

## 🎯 **Общая ценность системы для бизнеса**

### **Операционный контроль:**
- Ежедневный мониторинг остатков на всех счетах и депозитах
- Отслеживание исполнения платежного календаря
- Контроль ликвидности по организациям и БД

### **Финансовый анализ:**
- Анализ структуры денежных потоков по иерархии статей
- Выявление сезонных и трендовых закономерностей
- Планирование денежных потоков на основе исторических данных

### **Управленческая отчетность:**
- Единая система классификации статей ДДС
- Многоуровневая аналитика от детальных данных до сводных отчетов
- Интеграция данных о движении и остатках денежных средств

### **Технические преимущества:**
- **Полнота данных**: учет всех счетов из обеих таблиц-источников
- **Производительность**: оптимизированные запросы с UNION DISTINCT
- **Масштабируемость**: поддержка растущего объема данных
- **Гибкость**: возможность адаптации под различные аналитические потребности

---

## 🔄 **Взаимосвязь между отчетами**

1. **Ostatki.sql** → Детальный ежедневный отчет с движением средств (основной отчет)
2. **Ostatki_otchet.sql** → Упрощенный агрегированный отчет для быстрого анализа
3. **Table_hierarcy.sql** → Справочник для группировки и анализа статей ДДС

**Типичный сценарий использования:**
1. Используйте **Table_hierarcy.sql** для понимания структуры статей ДДС
2. Анализируйте детальные данные в **Ostatki.sql**
3. Получайте сводную информацию в **Ostatki_otchet.sql**

Эта система позволяет **полноценно управлять аналитикой денежных средств** от операционного контроля до стратегического планирования.
```
